{
  "name": "step-808",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "A browser drum sequencer built with React, TypeScript and the Web Audio API.",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && rm -rf ../public/drums && cp -r dist ../public/drums && echo '\\n✓ Built and copied to ../public/drums — commit the change to deploy.'"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.1",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}
